general enchancements
create an dashboard with usefull insights and make it as main page post login
use mui table everywhere
ui should only use yellow and black scheme similar to nool app
make the oveall app premium no css faults keep it clean and neat
button should have icons it should not be dimple also make the ui rich and premium


teachers
-provode option to delete and multi delete and deactivate should be posible
-firterable sortable has to be implemented
-show option to send the genereated password through mail to the user with some prefilled content admin will take call to send
-when user does the bulk upload it should return the generated password as excel list.

student 
-provode option to delete and multi delete and deactivate should be posible
-firterable sortable has to be implemented
-show option to send the genereated password through mail to the user with some prefilled content admin will take call to send
-when user does the bulk upload it should return the generated password as excel list.

Classes 
- enable crud operations
-provode option to delete and multi delete and deactivate should be posible
-firterable sortable has to be implemented
- classes can only be till 12th

subject
- allow to add subjects
